# SkyblockAddon Phase 9 Verification - 2026-07-05

## Verdict: YELLOW

Phase 9 source parity with official upstream commit `35af3ff` is complete. No unresolved critical or high source finding remains. The verdict stays yellow because the new Ars Nouveau portal hook and the complete optional-mod matrix have not run on a staging server with live island data.

## Critical

None found.

## High

None found.

## Medium

- [ ] Run Ars Nouveau Warp Portal behavior for allowed member, denied member, visitor, owner, OP, and absent-mod deployments on staging.
- [ ] Rehearse schema-v2 permission migration against copied live `world/islanddata` and restart twice.
- [ ] Inventory the production server JAR directory and complete the remaining optional-mod runtime matrix.

## Low

- [ ] Track the external ForgeGradle 6.0.54 use of Gradle's deprecated `ResolvedConfiguration.getFirstLevelModuleDependencies(Spec)` API.

## Verified Clean

- Upstream permissions: Industrial Foregoing and Sophisticated Storage use the new granular IDs and exact installed registry names.
- Permission migration: schema version 2 copies `mod_industrialforegoing`, `open_sophstorage`, and `mod_ars_nouveau` values into every replacement without overriding explicitly stored replacement values.
- Optional gate: `creeper_power` was corrected to upstream's `creeperpower` mod ID.
- Ars Nouveau API: the redirect target and `PortalTile.tick()` entity-query path were verified against the exact deployed Ars Nouveau 2.9.0 JAR, SHA-256 `35aa0b3ecb218e3355d99d7302bf3f633b982f9072b39abcdb0fc176ebeecdff`.
- Ars permission semantics: a dedicated `ars_nouveau_portal` permission now claims `ars_nouveau:portal` for `onEnterPortal`. This closes the missing rule in the upstream commit.
- Optional loading: the Ars mixin configuration is registered only when `PortalTile` is linkable, and no Ars package is embedded in SkyblockAddon.
- Migrated configuration: 71 legacy permissions convert to 100 final permissions and 90 group declarations, with 15 retired legacy IDs, zero custom IDs lost, and zero retired IDs active.
- Automated verification: 45 Java tests and one Python converter test pass with zero failures.
- Build: clean `buildAll --warning-mode all` passes. The only warning is the documented external ForgeGradle deprecation.
- Artifact: 11 mixin configurations are present and no optional dependency packages are bundled.
- Artifact path: `1.18.2/build/libs/skyblockaddon-10.0.0.jar`, 578,855 bytes, SHA-256 `c1eff3d694031abc15efeb24ff4164a0f14a4cc0d3f0f43f8baa438d4c73145e`.

## Recommended Next Actions

1. Copy production `world/islanddata` and the production mod inventory into staging.
2. Run the schema-v2 migration and restart checks.
3. Complete the Ars portal and remaining optional-mod runtime matrix before production deployment.
